# Day 6 – Object-Oriented Programming & Constructors

## Topics Covered
- Introduction to object-oriented programming (OOP)
- Classes and objects in Python
- Constructors (`__init__` method)
- Problem solving using classes and constructors

## Key Notes
- OOP helps structure code around objects that have attributes (data) and methods (behavior).
- A class defines a blueprint; objects are instances of the class.
- Constructor (`__init__`) runs automatically when an object is created and initializes attributes.
- Syntax:
  ```python
  class Person:
      def __init__(self, name, age):
          self.name = name
          self.age = age
  ```
- `self` refers to the current instance of the class.

## Example Concepts / Pseudo-Code
- Class for `Student` with name, roll number, and marks.
- Constructor used to initialize student details.
- Methods to display student info or calculate grade.

## What I Practiced
- Creating classes with multiple attributes.
- Using constructors to set initial values.
- Writing simple methods and calling them on objects.

## Reflection
- I understand the basics of classes, objects, and constructors in Python.
- OOP will help in organizing bigger programs and real-world models.
- I need more practice building small OOP-based solutions (like bank account, library, etc.).
